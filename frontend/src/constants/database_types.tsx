export enum SourceTypeEnum {
  PostgreSQL = "POSTGRESQL",
  MySQL = "MYSQL",
  Supabase = "SUPABASE",
}

export enum ColumnDataType {
  BOOL = "BOOL",
  UUID = "UUID",
  TEXT = "TEXT",
  VARCHAR = "VARCHAR",
  TIMESTAMP = "TIMESTAMP",
  TIMESTAMPTZ = "TIMESTAMPTZ",
  INT4 = "INT4",
  INT8 = "INT8",
}
