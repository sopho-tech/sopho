export enum SourceTypeEnum {
  PostgreSQL = "POSTGRESQL",
  Sqlite = "SQLITE",
}

export enum ColumnDataType {
  BOOL = "BOOL",
  UUID = "UUID",
  TEXT = "TEXT",
  VARCHAR = "VARCHAR",
  TIMESTAMP = "TIMESTAMP",
  TIMESTAMPTZ = "TIMESTAMPTZ",
  INT4 = "INT4",
  INT8 = "INT8",
}
